# weekly · 产品需求文档

> 状态：待评审
> 部署：阿里云 ECS，`http://weekly.gouxinjie.com`
> 日期：2026-09-18

---

## 一、产品定位

**产品名：weekly**（项目代号与产品名一致）

一个**以周为单位**的工作记录工具。核心是把「每周写一次总结」这件事做得足够顺手。

**两个独立模块**：周报（时间轴记录）、待办（待办清单）。各自独立，可以只用其中一个。

**多用户**：任何人可通过注册入口创建账号，每人拥有完全独立、互不可见的周报与待办数据。

**设计原则**

1. 不强制日更。一周一篇，写多写少都由用户决定。
2. 模块解耦。模块间只允许显式的单向操作，不做隐式关联。
3. 时间轴固定从 2026 年开始，不可修改。
4. 数据隔离是硬约束：任何查询都必须带 `user_id` 条件。

---

## 二、技术形态

| 项 | 方案 |
|---|---|
| 部署环境 | 阿里云 ECS |
| 访问域名 | `weekly.gouxinjie.com` |
| 协议 | HTTP（不使用 HTTPS，风险已知并接受） |
| 反向代理 | Nginx 转发到 Node 内网端口 |
| 数据层 | SQLite，文件位于 `data/weekly.db` |
| 登录方式 | 手机号 + 密码（无短信验证码） |
| 用户模型 | 多用户，注册入口长期开放，按 `user_id` 隔离 |
| 会话有效期 | 30 天 |
| 进程守护 | systemd |

### 技术栈

**代码组织：单仓双目录**，前后端各自 `package.json`、各自构建。

```
weekly/
├── server/              # Fastify API 服务
│   └── src/
│       ├── db/          # 所有 SQL 集中于此
│       ├── routes/      # 路由层
│       ├── middleware/  # 会话校验、user_id 注入
│       └── index.ts
├── web/                 # React SPA
│   └── src/
│       ├── pages/       # 4 条路由对应页面
│       ├── components/  # 各组件附带同名 .module.css
│       ├── styles/      # variables.css 设计变量
│       └── api/         # fetch 薄封装
└── data/weekly.db
```

| 后端 | 选型 |
|---|---|
| 运行时 | Node.js 22 LTS + TypeScript |
| Web 框架 | Fastify |
| 数据库 | SQLite（`better-sqlite3`），WAL 模式 |
| 查询方式 | 手写 SQL + 薄封装 |
| 密码哈希 | argon2 |
| 会话 | 自建 token + `session` 表 |
| 日期处理 | dayjs + isoWeek 插件 |

| 前端 | 选型 |
|---|---|
| 构建工具 | Vite |
| 框架 | React 19 + TypeScript |
| 路由 | React Router |
| 样式 | CSS Modules + 全局 CSS 自定义属性 |
| Markdown 编辑 | TipTap（所见即所得，底层仍存 Markdown） |
| Markdown 渲染 | markdown-it（必须开启 HTML 转义，仅用于预览模式） |
| 数据请求 | 原生 fetch + 薄封装 |
| 状态管理 | `useState` + Context |

**实现约定**

- SQLite 的 row 类型与 session 对象必须显式建模
- 所有 SQL 集中在 `server/src/db/`，`user_id` 条件写在明面上
- 不用 ORM、JWT、状态管理库、UI 组件库


### ⚠️ 已知并接受的风险：不使用 HTTPS

服务通过 HTTP 提供，**凭据、内容均明文传输**；公网扫描会尝试攻击登录 / 注册接口；会话 Cookie 被截获可直接复用。因为是多用户共享，任一处失守影响的是**全部注册用户**。

**补偿措施（不可省略）**

1. 密码 argon2 哈希存储，绝不存明文
2. 登录失败次数限制 + IP 限流
3. 注册接口加限流，防批量灌水
4. 会话 Cookie 设置 HttpOnly + SameSite
5. 密码强制「数字 + 字母」组合，长度 ≥ 6 位
6. 接口不泄露用户是否存在（防手机号枚举）
7. ECS 安全组只开放必要端口，Node 仅监听内网

**升级路径**：阿里云免费 DV 证书 → Nginx 增加 `listen 443 ssl` → 加 HTTP 跳转 → Cookie 追加 `Secure`。改动量小，建议尽早执行。

**SQLite 的连带收益**：备份即复制 `.db` 文件；自带 FTS5，搜索可直接启用。

---

## 三、核心概念与关系

| 概念 | 定义 | 创建方 | 可否重命名 |
|---|---|---|---|
| 年节点 | 2026 年 | 系统自动 | 否 |
| 月节点 | 9 月 | 系统自动（按该周的周四归属） | 否 |
| 周节点 | 第 38 周 · 09/14–09/20 | 系统自动（ISO 8601） | 否 |
| 周报 | 某一周的 Markdown 文档，一周一篇 | 用户 | 否（标题即周次） |
| 待办 | 待办条目，独立模块，可选标记所属周次 | 用户 | 是 |

**关键规则**

1. 年 / 月 / 周三级节点由系统推导，用户不能增删改。周节点即最小内容单元，其下无日节点。月份按该周的**周四**归属：周四是 ISO 周的代表日，也是本周天数最多所在的月份，因此 2026 年第 1 周（周一为 2025-12-29）归入 1 月，不会出现「1 月没有第 1 周」。
2. **两个模块彼此独立**。待办不隶属于周报或任何时间节点；「所属周次」只是可选标签，为 null 时不影响任何行为。
3. 模块间只允许一条弱连接：待办标记所属周次后，仅在周报侧作为参考素材显示，待办本身不受影响。
4. 时间轴起点固定为 2026 年，**不提供任何修改入口**（详见 R-11）。
5. **数据隔离**：`user_id` 由服务端从会话推导并强制注入，不接受前端传入（两条必守规则见下）。

**数据隔离的两个必守规则**

1. **服务端强制注入**：所有 SQL 的 `WHERE` 子句由服务端拼接 `user_id`，前端无权指定。
2. **写操作先校验归属**：按 `id` 更新 / 删除时必须写成 `WHERE id = ? AND user_id = ?`，不能只按 `id`——否则知道别人的记录 id 就能改别人的数据。

**数据存储约定**

- 一人一周一篇由数据库唯一约束保证：`UNIQUE (user_id, year, week)`。
- 待办的「所属周次」为可空字段，为 null 时不影响任何行为。
- 周次计算按 ISO 8601，周一为一周起点，用 `dayjs` 的 isoWeek 插件推导，不手写日历逻辑（同一周不跨年拆分，按 ISO 年归属）。
- 备份即复制 `data/weekly.db`；服务端定时 `VACUUM INTO` 生成一致性快照，配合 ECS 磁盘快照。
- 起点作为服务端常量（`START_YEAR = 2026`），服务端校验并拒绝任何早于 2026 年第 1 周的写入；跨年时自动把新年份加入树。

---

## 四、功能需求

### 4.1 周报模块

| 编号 | 需求 | 说明 | 优先级 |
|---|---|---|---|
| R-01 | 三层树导航 | 年 > 月 > 周，年与月均可折叠展开，折叠状态持久化；**未写的周同样出现在树中** | P0 |
| R-02 | 定位到本周 | 打开应用自动展开当年，选中本周 | P0 |
| R-03 | 上一周 / 下一周 | 编辑区顶部快捷切换 | P0 |
| R-04 | Markdown 编辑 | 编辑 / 预览双模式切换 | P0 |
| R-05 | 自动保存 | 输入停止 800ms 后落库，显示「已保存 / 保存中」 | P0 |
| R-06 | 写作状态角标 | 周节点显示「已写 / 未写」，未写的周为灰色 | P0 |
| R-07 | 全部展开 / 收起 | 一键操作当前年份下所有周 | P0 |
| R-08 | 周报模板 | 模板**不自动注入**，周报由用户从右栏「应用模板」按需注入（本周进展 / 遇到的问题 / 下周计划），可一键清空 | P1 |
| R-09 | 单周导出 | 导出 Markdown / 复制到剪贴板 | P1 |
| R-10 | 补写历史周 | 可跳到 2026 年第 1 周及其后任意周 | P1 |
| R-11 | 时间轴起点 | 固定从 2026 年开始，之前的年份不出现在树中 | P0 |
| R-12 | 字数统计 | 编辑区显示本周字数 | P2 |
| R-13 | 连续记录统计 | 显示已连续记录多少周 | P2 |
| R-14 | 年份归档 | 一整年写完后可标记归档，默认折叠 | P2 |

**R-08 模板内容**（用户点击「应用模板」时注入，会覆盖当前内容并二次确认）

```markdown
### 本周进展

### 遇到的问题

### 下周计划
```

**模板不自动注入**：未写过的周打开后编辑区保持空白，只有用户真正敲入内容才会落库并标记「已写」。
不能因为「点开看了一眼」就自动填入模板——那样自动保存会把模板写进库里，该周立刻变成「已写」。
判定口径统一为：**`content` 去空白后为空即不算已写**（服务端 `listWrittenWeeks` 与前端状态角标都按此判定）。

### 4.2 待办模块

**定位**：独立待办清单，不隶属于周报或任何时间节点。可只用待办、完全不用周报。

**内容形态**：纯待办条目（一行文本 + 勾选框），不承载长文本。

**组织方式**：单一清单 + 状态分组（置顶 / 未完成 / 已完成），不建文件夹、不做嵌套。

| 编号 | 需求 | 说明 | 优先级 |
|---|---|---|---|
| M-01 | 待办条目 | 一行文本 + 勾选框，回车即新建，支持编辑删除 | P0 |
| M-02 | 状态分组 | 三段分组，已完成默认折叠但保留 | P0 |
| M-03 | 标记所属周 | 标记 `year + week`；**新建时默认写入当前 ISO 周**，之后可改可清除；为 null 时不影响任何行为 | P0 |
| M-04 | 置顶 | 重要条目置顶到最上方 | P0 |
| M-05 | 拖拽排序 | 同组内可拖拽调整顺序 | P1 |
| M-06 | 清单筛选 | 四选一筛选：全部 / 本周 / 未完成 / 已完成，各项带计数。**「本周」按「所属周」判定**（手动标记优先，未标记按创建时间推导），与清单分组同一口径 | P1 |
| M-07 | 过期提示 | 标记周次已过且未完成 → 该条挂「过期」小标签（危险色）；不改分组、**不染整行底色**；仅视觉提示，无提醒推送 | P2 |
| M-08 | 从周报转待办 | 周报中选中一行转为待办 | P2 |
| M-09 | 计数角标 | 「待办」标签显示未完成条数 | P2 |

### 4.3 全局能力

| 编号 | 需求 | 说明 | 优先级 |
|---|---|---|---|
| G-01 | 注册 | 手机号 + 密码，入口长期开放；手机号唯一，重复注册拒绝。密码要求：**数字 + 字母组合，长度 ≥ 6 位** | P0 |
| G-02 | 登录鉴权 | 会话用 HttpOnly + SameSite Cookie，有效期 30 天 | P0 |
| G-03 | 密码安全存储 | argon2 哈希 | P0 |
| G-04 | 登录 / 注册限流 | 失败次数限制 + IP 限流 | P0 |
| G-05 | 忘记密码兜底 | 登录页常驻「忘记密码请联系 13113183859」，人工核对后重置 | P0 |
| G-06 | 数据隔离 | 所有读写强制带 `user_id`，服务端从会话推导 | P0 |
| G-07 | 空态与首次引导 | 无数据时给出上手入口，不显示空白面板 | P0 |
| G-08 | 修改密码 | 需验证原密码；改密后使其他会话失效；提交前需二次确认 | P1 |
| G-09 | 手动登出 | 清除当前会话；支持「登出所有设备」（清空该用户全部 session）；退出登录与退出所有设备均需二次确认 | P1 |
| G-10 | 快捷键 | Ctrl+S 立即保存、Ctrl+B 折叠 / 展开年节点、Ctrl+K 打开跳转到周次（G-17） | P1 |
| G-11 | 备份提示 | 设置页显示数据位置与备份方法 | P1 |
| G-12 | 键盘导航 | 上下键切换周次，Enter 进入编辑 | P2 |
| G-13 | 主题切换 | 设置页「外观」提供浅色 / 暖纸 / 深色三套主题，存本机 localStorage，不随账号同步（已实现，见 §四 设计变量） | P2 |
| G-14 | 概览首页 | 展示本年已写周数、待办未完成数（不在 MVP） | P2 |
| G-15 | 多标签页提示 | 检测到本页数据已在别处更新时提示刷新，仅提示不做实时同步 | P2 |
| G-16 | 窄屏提示 | 视口 < 1024px 时提示「请在桌面端使用」，不提供移动端布局 | P2 |
| G-17 | 跳转到周次 | 输入「2026 年第 15 周」直接定位并选中该周 | P1 |

**G-05 处理流程**：用户联系 → 核对身份 → 操作数据库重置密码哈希 → 通知用户。不做邮箱 / 密保找回。代价：依赖人工响应。

**错误反馈约定**：表单校验与保存失败一律**就地显示**（周报页长时间停留，Toast 容易被忽略）；仅跨区域的短反馈（导出成功、已登出所有设备）用顶部 Toast。

**数据隔离验证**：`db/` 层必须有跨用户访问测试——用 A 的会话调接口改 B 的记录 id，必须失败。用 Node 内置 `node --test`，不引入测试框架。这是本系统最严重的单点风险，不可省略。

**未来规划（移出 MVP）**

| 需求 | 说明 |
|---|---|
| 全局搜索 | SQLite 启用 FTS5 即可 |
| 数据导出 / 导入 | 复制 `.db` 文件即可 |
| 启用 HTTPS | 见 §二 升级路径 |
| 自助密码找回 | 需引入邮箱或短信服务 |
| 用户管理后台 | 只读用户列表 |
| 回收站 | 删除内容可恢复 |
| 概览首页 | 展示本年已写周数、待办未完成数（G-14） |
| 更多主题（如冷色系） | 三套主题已落地（G-13），新增只需加变量块 + 选项表 |
| 多标签页实时同步 | 需 WebSocket（G-15 当前仅提示） |
| 移动端布局 | 需重新设计骨架，当前只保证桌面端 |

---

## 五、界面结构

### 5.1 未登录态

访问 `weekly.gouxinjie.com` 首先看到登录页，登录 / 注册同页切换。注册入口**不设关闭条件**；注册页底部同样显示联系方式。

```
┌───────────────────────────────────────┐
│              weekly                   │
│     ┌───────────────────────────┐     │
│     │  登录 | 注册              │     │
│     │  手机号  [              ] │     │
│     │  密码    [              ] │     │
│     │  （注册时）确认密码 [    ] │     │
│     │  （注册时）数字+字母，≥6位   │     │
│     │      [    登  录    ]     │     │
│     │  忘记密码？请联系          │     │
│     │  13113183859              │     │
│     └───────────────────────────┘     │
└───────────────────────────────────────┘
```

登录失败提示统一为「手机号或密码错误」，不区分账号是否存在。

### 5.2 两种骨架

登录后界面只有**两种骨架**，切换标签页即整体换骨架。登录页**不属于骨架体系**——它是独立的全屏居中卡片，无左栏无右栏。

```
【周报态】
┌────────┬────────┬──────────────────┬──────────┐
│ 页签栏  │ 时间轴  │       中栏        │   右栏    │
│ 200px  │ 220px  │      自适应       │  220px   │
│        │        │                  │  可收起   │
│  页签   │ 三层树  │      编辑区       │ 本周待办  │
└────────┴────────┴──────────────────┴──────────┘

【待办态】
┌────────┬────────┬──────────────────┐
│ 页签栏  │ 筛选列  │       中栏        │
│ 200px  │ 220px  │      自适应       │
│  页签   │ 四项筛选 │       清单        │
└────────┴────────┴──────────────────┘
```

| 区域 | 周报态 | 待办态 |
|---|---|---|
| 页签栏 | 200px：logo + 页签 + 底部账号区 | 同左（宽度一致，位置不跳动） |
| 左列 | 220px：`时间轴` 标题 + 年 / 月 / 周三层树 | 220px：`筛选` 标题 + 全部 / 本周 / 未完成 / 已完成（行尾计数） |
| 中栏 | 标题栏 + 编辑区 + 工具条（最小 480px） | 标题 + 搜索 + 新建输入框 + 分组清单 |
| 右栏 | 「本周待办」220px，可收起 | **整栏移除**，不是收起 |

**关键规则**

1. 页签栏宽度两态一致（200px），切换时页签与账号区不跳动，只有右侧内容换。
2. 左列两态都在且同宽（220px），只是内容不同：周报态是时间轴三层树，待办态是筛选列表。
3. 左列与中栏共享顶栏——周报态的年份切换落在左列正上方，与 `时间轴` 标题左对齐。
4. 待办态下没有右栏，中栏因此比周报态更宽。
5. 窗口变窄时优先压缩右栏，其次才出现横向滚动。
6. 页签在页签栏顶部，**整个应用没有顶部导航栏**。
7. 设置页也不属于两种骨架——它是单列居中布局，从页签栏的「设置」页签进入。

### 5.3 左栏两个标签页

| 标签页 | 内容 |
|---|---|
| 周报 | 三层树（年 > 月 > 周），只读结构；**2026 年起所有周全部列出**，有内容的周显示绿色角标（`--color-saved`），未写的灰色（`--color-text-faint`）；默认只展开选中周所在的月份，月标题在该月有内容时，行尾右对齐显示「已写 / 全月」周数（如 `3/5`，等宽数字、`--color-saved`） |
| 待办 | 左列放筛选器：全部 / 本周 / 未完成 / 已完成，各项行尾带计数；不做树 |

两个标签互不依赖，可以只用其中一个。多用户不影响界面结构——数据来源在服务端限定，前端无感知。

### 5.4 中栏两种视图

| 视图 | 触发 | 结构 |
|---|---|---|
| 周报编辑 | 选中周节点 | 标题栏（周次 + 日期 + 保存态 + 字数）→ 编辑 / 预览区 → 工具条 |
| 待办清单 | 切到待办标签 | 顶部固定新建输入框 → 按所属周分组（新周在前）的清单 |

中栏无空白屏。周报态未写时编辑区保持空白，可直接书写；待办态输入框始终在，光标可直接落入。

### 5.5 右侧抽屉

可收起，收起后中栏全宽。仅在周报编辑态显示「本周待办」——标记在本周的待办条目，**只读展示，不可在此勾选或编辑**。待办态下整栏隐藏。

### 5.6 各区域元素清单

**左栏 · 周报态**

| 元素 | 行为 |
|---|---|
| 页签 | 切换骨架，周报态下高亮 |
| 年节点 | 可折叠，默认展开选中的年份 |
| 月节点 | 「9 月」可折叠，缩进一级；该月有已写周时补绿色圆点（`--color-saved`），一周未写时整行淡显 |
| 周节点 | 时间轴圆点 + 「第 38 周 · 09/14–09/20」；选中态为浅底圆角卡片，圆点放大成同心圆环（已写绿环、未写主色环） |
| 展开全部 / 收起全部 | 作用于当前年份下所有周 |
| 定位本周 | 快速跳回当前周（R-02）；已在本周时置灰 |
| 跳转到周次 | 输入「2026 年第 15 周」直接定位（G-17） |
| 底部账号区 | 头像 + 账号名 + 手机号脱敏（`138****1111`）；仅展示，登出统一在设置页（G-09） |

**左栏 · 待办态**

| 元素 | 行为 |
|---|---|
| 页签 | 待办态下高亮 |
| 筛选器四项 | 全部 / 本周 / 未完成 / 已完成，行尾右对齐计数；选中项为浅底圆角卡片 + 主色加粗文字 |
| 底部账号区 | 与周报态一致，位置不跳动 |

**中栏 · 周报态**

| 元素 | 行为 |
|---|---|
| 标题栏 | 「第 38 周 · 2026/09/14 – 09/20」 |
| 保存态 | 显示「保存中 / 已保存」（R-05） |
| 字数 | 显示本周字数（R-12） |
| 上一周 / 下一周 | 快捷切换（R-03） |
| 编辑 / 预览 | 双模式切换，保留滚动位置 |
| 编辑区 | 所见即所得编辑，底层为 Markdown |
| 工具条 | 编辑区上方：撤销 / 重做 / 标题（H1-H3）/ 加粗 / 斜体 / 删除线 / 无序·有序·待办列表 / 引用 / 代码块 / 分割线 / 表格 / 链接；只提供 Markdown 能无损表达的格式（不做字号、颜色、对齐） |

**右栏 · 周报态**

| 元素 | 行为 |
|---|---|
| 标题 | 「本周待办」 |
| ＋ 新建 | 跳转待办页并带上当前查看的周（`/todo?year=&week=`），新建的待办默认标记到该周 |
| 待办条目 | 只读，不可编辑（保持模块单向） |
| 空态 | 「本周暂无标记的待办」，附「去待办添加」，同样带上当前查看的周 |

**中栏 · 待办态**

| 元素 | 行为 |
|---|---|
| 新建输入框 | 常驻顶部，Enter 新建并保持焦点（M-01）；行内标出默认归属周。默认归属周取当前 ISO 周，从周报页带 `?year=&week=` 跳来时取该周（M-03）；需要归属其它周时创建后在「⋯」菜单改标记 |
| 分组标题 | 按**所属周**分组并倒序（手动标记的周优先，未标记按创建时间推导）：当周显示「本周 · 第 39 周（N）」，其余显示「第 38 周 · 26（N）」；组内置顶优先、创建先后 |
| 已完成分组 | 默认折叠，标题可点开 |
| 条目 | 勾选框 + 文本 + 可选小标签：置顶 / 分类 / 未标记 / 过期。周次已由分组标题表达，行上不再重复；未标记（浅色）表示不进周报「本周待办」，过期（危险色，M-07）表示标记的周已过且未完成；两种状态都不染整行底色 |
| 空态 | 「还没有待办，在上面输入一条试试」 |

### 5.7 页面清单

| 页面 | 路径 | 需登录 | 骨架 | 说明 |
|---|---|---|---|---|
| 登录 / 注册 | `/login` | 否 | 风景背景上的悬浮卡片（宽 420px，垂直居中） | 两页签切换；底部常驻联系方式 |
| 工作台（周报） | `/weekly/:year/:week` | 是 | 三栏 | 默认跳到本周 |
| 工作台（待办） | `/todo` | 是 | 两栏 | 主界面待办态 |
| 设置 | `/settings` | 是 | 单列居中（宽 `--settings-max-width`，当前 600px） | 改密码（G-08）、登出与退出所有设备（G-09）、备份说明（G-11） |

未登录访问受保护路径一律重定向到 `/login`，并带上原路径以便登录后跳回。`/weekly/` 不带参数时补全为当前 ISO 年的当前周。

### 5.8 空态规范

| 位置 | 触发条件 | 文案 | 附带动作 |
|---|---|---|---|
| 周报编辑区 | 本周未写 | 编辑区留空，不显示占位文案 | 模板可从右栏「应用模板」按需注入 |
| 右栏本周待办 | 本周无标记待办 | 「本周暂无标记的待办」 | 「去待办添加」文字链 |
| 待办清单 | 全部为空 | 「还没有待办」 | 指向顶部输入框 |
| 某筛选结果为空 | 如「本周」无条目 | 「本周没有待办」 | 「查看全部」文字链 |
| 年节点下无内容 | 该年一周未写 | 不显示空态，树本身即提示 | — |

---

## 六、已拍板决策记录

无遗留待定问题。以下为全部问题的决策记录，对应内容均已落进正文。

| 编号 | 问题 | 决策 |
|---|---|---|
| Q1 | 周次计算标准 | **严格 ISO 8601**（周一为一周起点） |
| Q2 | 跨年周如何归档 | **按 ISO 年归属**，一周不拆分 |
| Q3 | 数据规模 | 预期注册用户 10 人以内、数据跨度 3 年内（约 150 周）；**不做虚拟滚动**，若树超过 300 节点再优化 |
| Q4 | 已完成待办保留策略 | **永久保留**——「做完了什么」正是写周报的素材 |
| Q5 | 待办截止日提醒 | **仅标记周次**，过期未完成则周次标签转危险色；不做到期提醒 |
| Q6 | 首次打开是否预置示例 | **完全空态 + 引导**——预置假数据会让用户先做「删除」这件负功 |
| Q7 | 多标签页 | **仅检测提示**（「本页数据已在别处更新，刷新查看」），不做实时同步 |
| Q8 | 概览首页 | **不做**，登录后直接落在本周周报 |
| Q9 | ECS 规格与备份 | 最小规格即可；**手动快照 + cron 定时 `VACUUM INTO`**，不引入自动快照服务 |
| Q10 | 多端同时在线 | **允许多设备**（手机 + 电脑是真实场景）；「登出所有设备」可一次清空 |
| Q11 | 用户管理后台 | **直接改数据库**，不做后台页 |
| Q12 | 数据隔离验证 | **写跨用户访问测试**，仅覆盖 `db/` 层的归属校验；不引入完整测试框架，用 `node --test` 即可 |
| Q13 | 右栏是否可拖动调宽 | **固定 220px**，不吃拖拽与持久化的复杂度 |
| Q14 | API 文档 | **接口注释即文档**，不生成 OpenAPI——单人或小团队自用，读代码更快 |
| Q15 | 错误反馈形式 | **就地显示**，用于表单校验与保存失败；**顶部 Toast** 仅用于跨区域的短反馈（如导出成功） |
| Q16 | 主题 | **三套主题（浅色 / 暖纸 / 深色）已提供切换入口**，存本机 localStorage；不随账号同步，换设备回到默认主题 |
| Q17 | 窄屏降级 | **只保证桌面端（≥ 1024px）**，窄屏给出提示条；周报编辑本质是桌面场景 |
| Q18 | 周报编辑形态 | **所见即所得（TipTap）**，底层仍存 Markdown；预览用 markdown-it。CodeMirror 源码编辑方案作废 |

---

## 七、MVP 边界

**做**

- Fastify + TypeScript 服务部署到 ECS，systemd 守护
- React 19 + Vite 构建为静态产物，Nginx 托管
- 子域名 + Nginx 反向代理
- 注册 + 登录 + 密码哈希 + 限流（密码强制数字 + 字母、≥ 6 位）
- 多用户数据隔离
- 忘记密码兜底（页面展示 13113183859）
- 会话 30 天，支持主动登出
- SQLite 存于 `data/weekly.db`
- 两种骨架 + 登录页
- 三层树：2026 年起全部周次，年与月均可折叠展开 + 定位本周 + 跳转到周次（G-17）
- 周报所见即所得编辑（TipTap，底层 Markdown）+ 自动保存 + 按需注入模板（未写的周保持空白）
- 单周导出 Markdown
- 待办独立清单 + 状态分组 + 置顶 + 可选标记周次
- 设置页：外观主题（G-13）、改密码（G-08）、登出与登出所有设备（G-09）、备份说明（G-11）
- 首次引导与空态（不预置示例内容）
- 数据备份（`VACUUM INTO` + ECS 快照）
- `db/` 层跨用户访问测试（`node --test`，仅覆盖归属校验）
- 三套主题（浅色 / 暖纸 / 深色）与设置页切换入口（G-13）

**不做**

- HTTPS（风险已记录，升级路径已备）
- 完整测试体系与 UI 测试（只保留 `db/` 层隔离测试）
- 全局搜索、数据导出 / 导入、回收站
- 标准密码找回、用户管理后台
- 主题随账号同步、连续记录统计、概览首页
- 移动端布局、多标签页实时同步、虚拟滚动、到期提醒推送、API 文档生成
